# 환경변수 (API 키 절대 노출 금지)
.env

# 가상환경
venv/

# 파이썬 캐시
__pycache__/
*.pyc

# 생성된 임베딩/결과물 (voyage는 프로덕션용이라 Git에 포함 — 아래 목록에서 제외)
embeddings_cohere.json
embeddings_kure.json
projects/
usage_log.json

# DATA_DIR 로컬 폴백 경로 (Railway에서는 Volume 마운트 경로가 대신 쓰임)
data/

# Notion onboarding (local only)
.notion/