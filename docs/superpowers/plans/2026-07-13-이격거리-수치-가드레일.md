# 이격거리/간격 수치 가드레일 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 초안에 등장하는 이격거리/간격 수치 중 참고자료에 근거가 없는 값을
탐지하는 코드 레벨 체커(`find_unverified_clearance_values`)를 추가하고,
`generate_draft.py`의 후처리 파이프라인과 system_prompt에 통합한다.

**Architecture:** 기존 `find_unverified_citations()`와 동일한 "초안에
있는데 컨텍스트에 없으면 미검증" 방향의 순수 함수를 `common.py`에 추가하고,
`generate_draft.py`의 `_finalize_draft()`에서 기존 두 체커(인용 검증,
위험성 구간표 검증)와 나란히 세 번째 체크로 호출한다. 세 함수 모두 순수
문자열 처리이며 Claude/Voyage API를 호출하지 않으므로, 이번 계획의 모든
테스트는 API 비용 없이 로컬에서 완결된다.

**Tech Stack:** Python 3, 정규식(`re`) 기반 문자열 처리. 이 저장소는
pytest를 쓰지 않고, `test_worktype_citations.py`처럼 `assert` 기반의
독립 실행 스크립트(`python script.py`)로 검증하는 관례를 따른다 — 이번
계획도 동일한 관례를 따른다.

## Global Constraints

- 감시 대상은 이격거리·간격 계열로 한정한다(스펙 스코프, 인원수·무게·전압
  등은 제외).
- 앵커 키워드: `이격거리`, `이격`, `간격`, `여유 공간`, `안전거리`,
  `접근 한계거리` (스펙에 명시된 값 그대로 사용, 임의 추가/축소 금지).
- 수치+단위 정규식은 `cm|mm|m|미터|센티미터|센티` 알터네이션 순서를
  반드시 지킨다(`m`을 `cm`/`mm`보다 먼저 두면 부분 매칭 버그 발생).
- draft 청크 분리 기준은 `<br\s*/?>|\n` — 마침표 기준 문장 분리는 쓰지
  않는다(소수점 수치가 깨짐).
- 정규화 함수는 기존 `_normalize_citation`(공백 제거)을 재사용한다 —
  새 정규화 함수를 만들지 않는다.
- 이번 계획의 모든 검증은 API 호출 없이 로컬에서 수행한다. API를 호출하는
  회귀 테스트(`test_worktype_citations.py` 재실행 등)는 이 계획의 범위 밖이며,
  필요 시 별도로 사용자에게 제안 후 실행한다.

---

## Task 1: `find_unverified_clearance_values` 함수 추가 (common.py)

**Files:**
- Modify: `common.py` (기존 `find_unverified_citations` 함수 — 현재
  라인 230-239 — 바로 아래에 추가)
- Test: `test_clearance_checker.py` (신규, 저장소 루트 — API 호출 없는
  독립 실행 스크립트, `test_worktype_citations.py`와 동일한 관례)

**Interfaces:**
- Produces: `CLEARANCE_KEYWORDS` (list[str]), `DISTANCE_VALUE_PATTERN`
  (`re.Pattern`), `find_unverified_clearance_values(draft: str,
  reference_context: str) -> list[str]` — 이후 Task 2에서
  `generate_draft.py`가 그대로 import해서 쓴다.
- Consumes: `common.py`에 이미 있는 `_normalize_citation(citation: str) ->
  str` (공백 제거 정규화 함수, 라인 216-227)를 그대로 재사용한다.

### Step 1: 실패하는 테스트 먼저 작성

`test_clearance_checker.py`를 저장소 루트에 새로 만든다. 이 시점에는
`find_unverified_clearance_values`가 아직 없으므로 import 자체가
실패해야 정상이다.

```python
# -*- coding: utf-8 -*-
"""
find_unverified_clearance_values() 회귀 테스트 — API 호출 없이 로컬에서
완결되는 순수 함수 테스트. common.py나 이 체커의 앵커 키워드/정규식을
바꿀 때마다 실행할 것.

사용 예:
  python test_clearance_checker.py
"""
import sys

if hasattr(sys.stdout, "reconfigure"):
    sys.stdout.reconfigure(encoding="utf-8")

from common import find_unverified_clearance_values

# 2026-07-12 C-2 재관찰 테스트에서 실제로 나온 위험성평가표 초안의 발췌.
# 위험요인 6 — KB에 없는 "30cm"가 창작된 실제 사례.
DRAFT_FABRICATED_CLEARANCE = (
    "③ [관리적 대책] 지게차 포크 및 캐비닛 외곽 치수를 실측하고, "
    "통로 폭과의 여유 공간(좌우 각 최소 30cm 이상)을 사전 확인 "
    "<br>④ [관리적 대책] 케이블 트레이 높이와 캐비닛 최고점 높이를 "
    "비교하여 상부 간섭 여부 사전 확인"
)

# 위험요인 3 — 같은 초안에서 활선설비 이격거리는 수치 없이
# "사전 실측"으로만 서술된 개선된 케이스 (수치 자체가 없으므로 검사 대상 없음)
DRAFT_NO_NUMBER_CLEARANCE = (
    "④ [공학적 대책] 지게차 최대 하물 높이(포크 상단 포함)와 활선설비 "
    "사이의 이격거리를 사전 실측하고 안전 여유 확보 여부 확인"
)

# 위험요인 1 — 이격거리와 무관한 기계적 수치(스코프 밖, 앵커 키워드 없음)
DRAFT_UNRELATED_NUMBER = (
    "② [공학적 대책] 지게차 포크 높이를 주행 시 지면에서 15~20cm로 "
    "제한하여 하중 무게중심을 낮게 유지"
)

# 표준작업계획서_법정별표.txt(굴착작업 섹션)에 실제로 있는 문장 —
# "이격거리"와 "1m"이 함께 등장하고, 컨텍스트에도 동일하게 존재해야
# 정상적으로 통과(미검증 아님)해야 한다.
KB_GROUNDED_TEXT = (
    "매설물 인근 이격거리 기준: 중장비(굴착기 등)에서 인력굴착으로 "
    "전환해야 하는 기준은 굴착 깊이가 아니라 매설물까지의 수평 거리다. "
    "가스배관 등 주요 매설물 인근은 실무에서 통상 수평 1m 이내 구간을 "
    "인력굴착 전환 기준으로 참고하는 경우가 많다."
)


def check(name, draft, reference_context, expected):
    result = find_unverified_clearance_values(draft, reference_context)
    status = "PASS" if result == expected else "FAIL"
    print(f"[{status}] {name}: got={result} expected={expected}")
    return status == "PASS"


def run():
    results = []

    # 1. KB에 없는 "30cm"는 미검증으로 잡혀야 한다
    results.append(check(
        "창작된 이격거리(30cm) 탐지",
        DRAFT_FABRICATED_CLEARANCE,
        reference_context="",
        expected=["30cm"],
    ))

    # 2. 수치가 없으면 검사 대상 자체가 없다
    results.append(check(
        "수치 없는 이격거리 언급은 통과",
        DRAFT_NO_NUMBER_CLEARANCE,
        reference_context="",
        expected=[],
    ))

    # 3. 앵커 키워드가 없으면 스코프 밖 — KB에 없어도 안 잡혀야 한다
    results.append(check(
        "무관한 수치(앵커 키워드 없음)는 스코프 밖",
        DRAFT_UNRELATED_NUMBER,
        reference_context="",
        expected=[],
    ))

    # 4. 컨텍스트에 동일한 값이 있으면 정상 통과(오탐 아님)
    results.append(check(
        "KB에 근거 있는 이격거리(1m)는 통과",
        KB_GROUNDED_TEXT,
        reference_context=KB_GROUNDED_TEXT,
        expected=[],
    ))

    print()
    print("=" * 50)
    print("전체 결과:", "PASS" if all(results) else "FAIL (위 로그 확인)")


if __name__ == "__main__":
    run()
```

- [ ] `test_clearance_checker.py` 파일을 위 내용대로 저장소 루트에 생성한다.

### Step 2: 테스트 실행해서 실패 확인

Run: `python test_clearance_checker.py`

Expected: `ImportError: cannot import name 'find_unverified_clearance_values' from 'common'`
(또는 `ModuleNotFoundError` 계열 에러 — `find_unverified_clearance_values`가
아직 `common.py`에 없으므로 import 단계에서 실패해야 정상이다.)

- [ ] 위 명령 실행 후 정확히 이 이유로 실패하는지 확인한다.

### Step 3: 최소 구현 작성

`common.py`의 `find_unverified_citations` 함수 정의(현재 라인 230-239)
바로 다음, `chunk_text` 함수 정의(현재 라인 242) 바로 이전에 아래 코드를
추가한다:

```python
# 이격거리/간격 등 "일반 수치" 가드레일. find_unverified_citations와 반대
# 방향인 위험성 구간표 체커(find_broken_risk_score_ranges)와 달리, 이격거리는
# KB마다 값이 상황에 따라 달라 고정된 정답 목록을 만들 수 없다. 그래서 조문
# 인용 체커와 동일하게 "초안에 있는데 참고자료에 없으면 미검증"이라는
# 방향으로 설계한다. 감시 대상을 이격거리/간격 계열로 좁혀 오탐을 줄인다 —
# 앵커 키워드가 없는 수치(예: 지게차 포크 높이 15~20cm)는 애초에 스코프 밖이라
# 검사하지 않는다.
CLEARANCE_KEYWORDS = ["이격거리", "이격", "간격", "여유 공간", "안전거리", "접근 한계거리"]

# 단위 알터네이션 순서가 중요하다 - m을 cm/mm보다 먼저 두면 "50cm"의 앞부분만
# "5"+"0c"로 잘못 나뉘는 게 아니라, 아예 "cm"을 놔두고 뒤의 "m"만 단위로
# 인식해버리는 부분 매칭 버그가 생긴다.
DISTANCE_VALUE_PATTERN = re.compile(
    r"\d+(?:\.\d+)?\s*(?:cm|mm|m|미터|센티미터|센티)(?![a-zA-Z가-힣])"
)


def find_unverified_clearance_values(draft, reference_context):
    """
    draft를 <br>/줄바꿈 단위로 나눠, 이격거리 관련 앵커 키워드가 포함된
    조각에서만 수치+단위 패턴을 추출한다. 그 값이 reference_context에
    그대로 등장하지 않으면 미검증으로 반환한다 (find_unverified_citations와
    동일하게 소프트 경고 용도 — 생성을 막지 않고 사람이 확인하도록 표시만 함).
    """
    chunks = re.split(r"<br\s*/?>|\n", draft)
    draft_values = set()
    for chunk in chunks:
        if any(kw in chunk for kw in CLEARANCE_KEYWORDS):
            draft_values.update(
                _normalize_citation(m.group())
                for m in DISTANCE_VALUE_PATTERN.finditer(chunk)
            )
    if not draft_values:
        return []
    context_values = {
        _normalize_citation(m.group())
        for m in DISTANCE_VALUE_PATTERN.finditer(reference_context)
    }
    return sorted(draft_values - context_values)
```

- [ ] 위 코드를 `common.py`에 그대로 추가한다.

### Step 4: 테스트 실행해서 통과 확인

Run: `python test_clearance_checker.py`

Expected:
```
[PASS] 창작된 이격거리(30cm) 탐지: got=['30cm'] expected=['30cm']
[PASS] 수치 없는 이격거리 언급은 통과: got=[] expected=[]
[PASS] 무관한 수치(앵커 키워드 없음)는 스코프 밖: got=[] expected=[]
[PASS] KB에 근거 있는 이격거리(1m)는 통과: got=[] expected=[]

==================================================
전체 결과: PASS
```

- [ ] 4개 케이스 전부 PASS로 나오는지 확인한다. 하나라도 FAIL이면 정규식
      또는 앵커 키워드 목록을 다시 확인한다 — 절대 기댓값(`expected=`)을
      결과에 맞춰 바꾸지 않는다.

### Step 5: 커밋

```bash
git add common.py test_clearance_checker.py
git commit -m "feat: 이격거리/간격 미검증 수치 탐지 함수 추가

find_unverified_citations와 동일한 방향(초안에 있는데 참고자료에 없으면
미검증)으로 이격거리/간격 계열 수치를 감시. 앵커 키워드로 스코프를
좁혀 오탐을 방지."
```

- [ ] 위 커밋을 실행한다.

---

## Task 2: `generate_draft.py` 후처리 파이프라인에 통합

**Files:**
- Modify: `generate_draft.py` (import 구문 — 현재 라인 16-25 — 와
  `_finalize_draft` 함수 — 현재 라인 323-344)
- Test: `test_finalize_draft_local.py` (신규, 저장소 루트 — API 호출 없이
  `_finalize_draft`를 직접 호출하는 로컬 테스트. `_finalize_draft`는 이미
  생성된 draft 텍스트를 받아 후처리만 하는 순수 함수라 Claude API를
  호출하지 않는다.)

**Interfaces:**
- Consumes: Task 1이 만든 `find_unverified_clearance_values(draft,
  reference_context) -> list[str]` (`common.py`에서 import)
- Produces: `_finalize_draft`가 반환하는 `draft`(경고 3종이 모두
  append될 수 있는 최종 문자열)와 `warning`(경고 문자열 또는 None) —
  이 형태는 Task 2 이전과 동일하게 유지된다(반환 시그니처 변경 없음).

### Step 1: 실패하는 테스트 먼저 작성

`test_finalize_draft_local.py`를 저장소 루트에 새로 만든다. `_finalize_draft`는
`project_name=None`을 넘기면 `save_project_record`를 호출하지 않으므로
디스크에 아무것도 쓰지 않고, Claude/Voyage API도 호출하지 않는다 — 완전히
오프라인으로 검증 가능하다.

```python
# -*- coding: utf-8 -*-
"""
_finalize_draft()의 3중 경고(조문 인용/위험성 구간표/이격거리)가 모두
정상적으로 draft에 붙는지 API 호출 없이 로컬로 검증한다.

사용 예:
  python test_finalize_draft_local.py
"""
import sys

if hasattr(sys.stdout, "reconfigure"):
    sys.stdout.reconfigure(encoding="utf-8")

from generate_draft import _finalize_draft

# 참고자료(컨텍스트)에는 존재하지 않는 조문 번호, 물결표 빠진 구간표,
# 근거 없는 이격거리 수치를 모두 담은 합성 초안.
FAKE_DRAFT = (
    "위험성 점수 구간: 14 낮음 / 59 중간 / 10~25 높음\n"
    "위험요인: 국사 내 지게차 운행 <br>"
    "안전대책: 제999조에 따라 통로 폭과의 여유 공간(좌우 각 최소 30cm 이상)을 확보한다."
)
FAKE_CONTEXT = "이 현장은 정보통신공사 국사 장비 반입 작업이다."  # 위 세 값 전부 미포함


def run():
    draft, saved_record, warning = _finalize_draft(
        FAKE_DRAFT, FAKE_CONTEXT, linked_risk_context="",
        document_type="위험성평가표", project_info="테스트용 project_info",
        project_name=None, user_id=None,
    )

    checks = [
        ("saved_record는 project_name이 없으면 None", saved_record is None),
        ("조문 인용 경고 포함", "제999조" in (warning or "")),
        ("구간표 경고 포함", "1~4" in (warning or "") and "5~9" in (warning or "")),
        ("이격거리 경고 포함", "30cm" in (warning or "")),
        ("draft 끝에 경고 3종이 순서대로 append됨", warning is not None and draft.endswith(warning)),
    ]

    ok = True
    for name, passed in checks:
        print(f"[{'PASS' if passed else 'FAIL'}] {name}")
        ok = ok and passed

    print()
    print("=" * 50)
    print("전체 결과:", "PASS" if ok else "FAIL (위 로그 확인)")


if __name__ == "__main__":
    run()
```

- [ ] `test_finalize_draft_local.py` 파일을 위 내용대로 생성한다.

### Step 2: 테스트 실행해서 실패 확인

Run: `python test_finalize_draft_local.py`

Expected: "이격거리 경고 포함" 항목이 FAIL로 나온다 (아직 `_finalize_draft`가
`find_unverified_clearance_values`를 호출하지 않으므로 "30cm"에 대한 경고가
draft/warning에 없음). 나머지(조문 인용, 구간표) 항목은 이미 구현되어
있으므로 PASS로 나와야 한다.

- [ ] 정확히 "이격거리 경고 포함" 항목만 FAIL인지 확인한다. 다른 항목까지
      FAIL이면 Task 1이 제대로 커밋됐는지 먼저 확인한다.

### Step 3: `generate_draft.py`에 통합

`generate_draft.py`의 import 블록(현재 라인 16-25)을 수정한다:

```python
    claude_client,
    DATA_DIR,
    DOCUMENT_TYPE_KB_MAP,
    FULL_INCLUDE_DOCUMENT_TYPES,
    read_kb_file,
    WORK_TYPE_KB_FILE,
    WORK_TYPE_SECTION_MARKERS,
    get_work_type_context,
    find_unverified_citations,
    find_broken_risk_score_ranges,
    find_unverified_clearance_values,
)
```

`_finalize_draft` 함수(현재 라인 323-356)의 구간표 체크 바로 다음에
세 번째 체크를 추가한다. 함수 전체를 아래 내용으로 교체한다:

```python
def _finalize_draft(draft, context, linked_risk_context, document_type, project_info, project_name, user_id):
    """
    스트리밍이 끝난 뒤든 non-streaming 응답을 받은 직후든, draft 텍스트가
    확정된 다음에 공통으로 수행하는 후처리(인용 검증 경고 부착 + 현장 기록
    저장). (최종 draft, saved_record)를 반환.
    """
    unverified = find_unverified_citations(draft, context + linked_risk_context)
    warning = None
    if unverified:
        print(f"[WARN] 참고자료에서 확인되지 않은 조문/별표 인용: {unverified}")
        warning = (
            "\n\n> ⚠ **자동 검증 알림**: 이 초안에 참고자료로 확인되지 않은 법령 조항 번호가 "
            f"포함되어 있습니다 ({', '.join(unverified)}). 국가법령정보센터에서 정확한 "
            "조번호를 반드시 재확인하세요."
        )
        draft += warning

    broken_ranges = find_broken_risk_score_ranges(draft)
    if broken_ranges:
        print(f"[WARN] 위험성 점수 구간 표기 누락(물결표 확인 필요): {broken_ranges}")
        range_warning = (
            "\n\n> ⚠ **자동 검증 알림**: 위험성 점수 구간 표기 중 일부가 원래 형식(물결표 "
            f"포함)으로 확인되지 않습니다 ({', '.join(broken_ranges)}). '1~4', '5~9', "
            "'10~25' 형식이 맞는지 반드시 재확인하세요."
        )
        draft += range_warning
        warning = (warning or "") + range_warning

    unverified_clearances = find_unverified_clearance_values(draft, context + linked_risk_context)
    if unverified_clearances:
        print(f"[WARN] 참고자료에서 확인되지 않은 이격거리/간격 수치: {unverified_clearances}")
        clearance_warning = (
            "\n\n> ⚠ **자동 검증 알림**: 이 초안에 참고자료로 확인되지 않은 이격거리/간격 "
            f"수치가 포함되어 있습니다 ({', '.join(unverified_clearances)}). 현장 실측값으로 "
            "반드시 재확인하세요."
        )
        draft += clearance_warning
        warning = (warning or "") + clearance_warning

    saved_record = None
    if project_name and user_id:
        saved_record = save_project_record(user_id, project_name, document_type, project_info, draft)

    return draft, saved_record, warning
```

- [ ] import 블록과 `_finalize_draft` 함수를 위 내용대로 수정한다.

### Step 4: 테스트 실행해서 통과 확인

Run: `python test_finalize_draft_local.py`

Expected:
```
[PASS] saved_record는 project_name이 없으면 None
[PASS] 조문 인용 경고 포함
[PASS] 구간표 경고 포함
[PASS] 이격거리 경고 포함
[PASS] draft 끝에 경고 3종이 순서대로 append됨

==================================================
전체 결과: PASS
```

- [ ] 5개 항목 전부 PASS인지 확인한다.

### Step 5: 커밋

```bash
git add generate_draft.py test_finalize_draft_local.py
git commit -m "feat: 이격거리 미검증 수치 체커를 _finalize_draft에 통합

find_unverified_clearance_values를 조문 인용/구간표 체커와 나란히
연결. 스트리밍/비스트리밍 양쪽 경로 모두 warning 변수를 공유하므로
자동으로 적용됨."
```

- [ ] 위 커밋을 실행한다.

---

## Task 3: system_prompt 가드레일 문구 추가

**Files:**
- Modify: `generate_draft.py` (system_prompt 문자열 — 현재 라인 279-290의
  (A)/(B) 조문 규칙 문단 바로 뒤)
- Test: `test_system_prompt_static.py` (신규, 저장소 루트 — 소스 파일을
  텍스트로 읽어서 문자열 존재만 확인하는 정적 검사. `_build_generation_prompt`를
  직접 호출하면 RAG 검색이 Voyage 임베딩 API를 호출하므로, 이를 피하기
  위해 함수를 실행하지 않고 소스 텍스트만 검사한다.)

**Interfaces:**
- Consumes: 없음(system_prompt는 `generate_draft.py` 내부의 고정 문자열).
- Produces: 없음(문구 추가만 하는 작업이라 다른 태스크가 이 결과물을
  가져다 쓰지 않는다).

### Step 1: 실패하는 테스트 먼저 작성

```python
# -*- coding: utf-8 -*-
"""
generate_draft.py의 system_prompt에 이격거리 가드레일 문구가 포함돼
있는지 정적으로 확인한다. _build_generation_prompt()를 직접 호출하면
RAG 검색이 Voyage 임베딩 API를 호출하게 되므로, 그 호출을 피하기 위해
소스 파일 텍스트만 읽어서 검사한다 (API 비용 없음).

사용 예:
  python test_system_prompt_static.py
"""
import sys

if hasattr(sys.stdout, "reconfigure"):
    sys.stdout.reconfigure(encoding="utf-8")

with open("generate_draft.py", "r", encoding="utf-8") as f:
    source = f.read()

EXPECTED_PHRASES = [
    "이격거리·간격 등 안전거리 수치를 다룰 때도",
    "현장 실측 후 확보",
]


def run():
    ok = True
    for phrase in EXPECTED_PHRASES:
        found = phrase in source
        print(f"[{'PASS' if found else 'FAIL'}] system_prompt에 '{phrase}' 포함")
        ok = ok and found

    print()
    print("=" * 50)
    print("전체 결과:", "PASS" if ok else "FAIL (위 로그 확인)")


if __name__ == "__main__":
    run()
```

- [ ] `test_system_prompt_static.py` 파일을 위 내용대로 생성한다.

### Step 2: 테스트 실행해서 실패 확인

Run: `python test_system_prompt_static.py`

Expected: 두 문구 모두 FAIL (아직 system_prompt에 추가되지 않았으므로).

- [ ] 2개 항목 전부 FAIL인지 확인한다.

### Step 3: system_prompt에 가드레일 문구 추가

`generate_draft.py`의 system_prompt 문자열에서, (A)/(B) 조문 규칙 문단이
끝나는 지점(현재 라인 288-290, "그 목록의 순번이 실제 법령의 '호' 번호와
같다는 보장은 참고자료에 없다."로 끝나고 "(A)와 (B)는 서로 다른 상황에..."로
이어지는 부분) 바로 뒤에 아래 문단을 추가한다:

```python
        "(A)와 (B)는 서로 다른 상황에 적용되는 규칙이야 — 참고자료에 있는 걸 빼는 것도, "
        "참고자료에 없는 걸 지어내는 것도 둘 다 잘못이야.\n\n"
        "이격거리·간격 등 안전거리 수치를 다룰 때도 위 (A)/(B) 규칙을 그대로 적용해. "
        "참고자료에 구체적인 거리·간격 값이 명시되어 있으면 그 값을 그대로 사용하고, "
        "명시되어 있지 않으면 구체적인 숫자를 만들어내지 말고 '현장 실측 후 확보'처럼 "
        "서술해.\n\n"
        "비상연락처·전화번호도 (A)/(B) 규칙을 그대로 적용해. 참고자료에 명시된 연락처(예: "
```

(기존 "비상연락처·전화번호도..." 문단 앞에 새 문단을 끼워 넣는 형태 —
기존 문단 순서와 내용은 그대로 유지하고 그 사이에 삽입한다.)

- [ ] 위와 같이 system_prompt 문자열을 수정한다. 기존 "(A)와 (B)는 서로
      다른 상황에..." 문단과 "비상연락처·전화번호도..." 문단 사이에 새
      문단이 정확히 한 번만 삽입됐는지(중복 삽입 없는지) 확인한다.

### Step 4: 테스트 실행해서 통과 확인

Run: `python test_system_prompt_static.py`

Expected:
```
[PASS] system_prompt에 '이격거리·간격 등 안전거리 수치를 다룰 때도' 포함
[PASS] system_prompt에 '현장 실측 후 확보' 포함

==================================================
전체 결과: PASS
```

- [ ] 2개 항목 전부 PASS인지 확인한다.
- [ ] `python -c "import generate_draft"`를 실행해 문법 오류가 없는지도
      확인한다.

### Step 5: 커밋

```bash
git add generate_draft.py test_system_prompt_static.py
git commit -m "feat: system_prompt에 이격거리 수치 창작 금지 가드레일 추가

기존 조문 인용 (A)/(B) 규칙과 동일한 원칙을 이격거리/간격 수치에도
적용 - 참고자료에 없으면 지어내지 말고 현장 실측 필요로 서술하도록 지시."
```

- [ ] 위 커밋을 실행한다.

---

## Task 4: 핸드오프 문서 갱신

**Files:**
- Modify: `docs/handoff_성능개선_및_검토사항.md` (C-2 섹션 및 "진행 순서
  제안" 목록)

**Interfaces:**
- Consumes: 없음(문서 작업).
- Produces: 없음.

### Step 1: C-2 섹션에 조치 완료 기록 추가

`docs/handoff_성능개선_및_검토사항.md`에서 C-2 섹션의 "C-3 완료 후
재관찰(2026-07-12, ...)" 단락(가장 마지막 단락) 바로 뒤에 아래 내용을
추가한다:

```markdown

**후속 조치 완료(2026-07-13)**: [docs/superpowers/specs/2026-07-13-이격거리-수치-가드레일-design.md](superpowers/specs/2026-07-13-이격거리-수치-가드레일-design.md)
설계에 따라 `find_unverified_clearance_values()` 추가 완료. 조문 인용
체커와 동일한 방향(초안에 있는데 참고자료에 없으면 미검증)으로 이격거리/
간격 계열 수치를 감시하며, system_prompt에도 동일 원칙의 가드레일 문구를
추가함. "일반 수치 가드레일 부재"라는 근본 진단에 대한 코드 레벨 대응이
이제 존재함(단, 스코프는 이격거리/간격으로 한정 — 인원수·무게 등 다른
수치는 여전히 스코프 밖).
```

- [ ] 위 내용을 정확히 그 위치에 추가한다.

### Step 2: "진행 순서 제안" 목록 갱신

같은 파일의 "진행 순서 제안" 목록 마지막(8번 항목) 뒤에 9번 항목을
추가한다:

```markdown
9. ~~일반 수치(이격거리/간격) 가드레일 설계 및 구현~~ — **완료
   (2026-07-13)**. `find_unverified_clearance_values()` 추가, system_prompt
   가드레일 문구 추가. 인원수·무게 등 다른 종류의 일반 수치는 여전히
   스코프 밖으로 남겨둠 — 필요 시 별도 스코프로 재검토
```

- [ ] 위 항목을 목록 끝에 추가한다.

### Step 3: 커밋

```bash
git add "docs/handoff_성능개선_및_검토사항.md"
git commit -m "docs: 이격거리 가드레일 구현 완료 핸드오프 문서 반영"
```

- [ ] 위 커밋을 실행한다.

---

## 완료 후 남는 것 (이 계획의 범위 밖)

- API 비용이 드는 회귀 확인(`test_worktype_citations.py` 재실행, 또는
  C-2 시나리오 재재현)은 이 계획에 포함하지 않는다. 구현 완료 후 사용자에게
  실행 여부를 별도로 제안한다.
- 인원수·무게 등 다른 종류의 일반 수치 가드레일은 스코프 밖 — 필요성이
  확인되면 별도 스펙/계획으로 진행한다.
