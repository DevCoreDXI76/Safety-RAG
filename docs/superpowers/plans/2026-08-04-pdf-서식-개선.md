# PDF 서식 개선 (제목·박스제목·열폭 자동조절) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PDF 내보내기(`export_pdf.py`)의 문서 제목·박스(섹션) 제목·표 열 너비·페이지 분할을 사용자가 요청한 서식으로 맞춘다. XLSX/HWPX는 이번 스코프에서 건드리지 않는다(사용자가 명시적으로 "PDF만 먼저"를 선택함).

**Architecture:** `export_pdf.py`가 `markdown_tables.py`의 파서 결과를 reportlab Flowable로 바꾸는 구조는 그대로 유지한다. 이번 변경은 3곳에 집중된다: (1) 문서 제목을 `Paragraph` 대신 커스텀 `Flowable`로 그려 굵게+밑줄+가운데정렬을 구현, (2) `markdown_tables.py`에 헤딩까지 포함해 순서대로 뽑아내는 새 함수를 추가해 표 앞에 "박스 제목"을 렌더링, (3) `document_styles.py`의 정적 비율 대신 실제 셀 텍스트 길이를 측정해 열 너비를 배분하는 content-aware 방식으로 교체.

**Tech Stack:** reportlab(Platypus/Flowable/pdfmetrics), 기존 `markdown_tables.py` 정규식 파서, pypdf(테스트 검증용).

## Global Constraints

- 이번 스코프는 **PDF만** — `export_xlsx.py`/`export_hwpx.py`/`document_styles.py`의 기존 정적 비율(`column_widths`)은 건드리지 않는다. 두 포맷은 계속 정적 비율을 쓴다.
- CID 폰트(`HYSMyeongJo-Medium`)에는 검증된 Bold 변형이 없다(export_pdf.py 파일 상단 docstring에 이미 기록됨) — 제목의 "굵게"는 같은 텍스트를 미세하게 겹쳐 그리는 faux-bold로 구현한다.
- 표 셀 텍스트를 escape 없이 그리는 곳(제목)과 escape가 필요한 곳(표 셀, `xml.sax.saxutils.escape`)을 혼동하지 않는다 — `Paragraph`에 넣는 텍스트만 escape가 필요하고, `canvas.drawString`에는 escape하면 안 된다(그대로 리터럴 텍스트로 나가버림).
- 모든 새 코드는 기존 저장소 테스트 컨벤션(`checks.append((설명, bool))` + PASS/FAIL 출력, `if __name__ == "__main__": run()`)을 따른다.
- 각 태스크 종료 시 관련 테스트를 직접 실행해 통과를 확인하고 커밋한다(파일 안 만들고 넘어가지 말 것).

---

### Task 1: 열 너비를 실제 셀 내용 길이 기반으로 자동 배분 (요구사항 5)

**Files:**
- Modify: `export_pdf.py` (`_build_table_element` 및 새 헬퍼 함수)
- Modify: `test_export_pdf.py` (새 케이스 추가)
- Modify: `test_export_style_consistency.py` (PDF 열비율=스펙비율 검증을 PDF의 새 동작에 맞게 교체)

**Interfaces:**
- Produces: `_measure_max_line_width(text: str, font_size: float) -> float`, `_content_aware_col_widths(raw_rows: list[list[str]], ncols: int, frame_width: float, font_size: float) -> list[float]` (둘 다 `export_pdf.py` 모듈 함수, 다른 파일에서 import하지 않음)
- Consumes: 기존 `_FONT_NAME`, `pdfmetrics.stringWidth`

배경: `document_styles.STYLE_SPECS["표준 작업계획서"]`는 4개 열 비율만 정의돼 있는데, LLM이 12열 표(작업유형별 법정 별표 구조)를 만들면 정의 안 된 8개 열이 `DEFAULT_COLUMN_WIDTH`로 채워지며 특정 열이 극단적으로 좁아지는 문제가 있었다(이미 `_fit_cell_text`로 "죽지 않게"는 막아뒀다 — 이번 태스크는 "애초에 안 좁아지게"가 목표). 이번 태스크는 정적 비율 대신, 표에 실제로 들어간 텍스트의 렌더 폭을 측정해서 짧은 열(예: "예/아니오")은 좁게, 긴 열은 넓게 배분한다.

- [ ] **Step 1: 실패하는 테스트 작성 — "예/아니오"만 있는 짧은 열은 좁고, 서술형 열은 넓어야 한다**

`test_export_pdf.py`에 아래를 `SAMPLE_RECORD_WITH_SCORE` 정의 아래, `run()` 함수 앞에 추가:

```python
SAMPLE_RECORD_MIXED_WIDTH = {
    "document_type": "TBM 일지",
    "draft": (
        "| 확인항목 | 이행여부 |\n"
        "|------|------|\n"
        "| 오늘 작업은 지중 굴착 및 광케이블 매설로, 굴착 깊이 2미터를 초과하는 구간이 "
        "포함되어 있어 흙막이 지보공 설치와 붕괴 위험 점검이 필수적으로 선행되어야 함 | 예 |\n"
    ),
}
```

`run()` 안, 마지막 체크(`checks.append(("좁은 열(12열 표 1열)...`) 바로 뒤에 추가:

```python
    # --- 열 너비가 실제 내용 길이에 비례해 배분됨(짧은 "예" 열은 좁고, 서술형 열은 넓음) ---
    mixed_tables = parse_markdown_tables(SAMPLE_RECORD_MIXED_WIDTH["draft"])
    mixed_flowable, _ = _build_table_element(
        mixed_tables[0], frame_width, SAMPLE_RECORD_MIXED_WIDTH["document_type"]
    )
    mixed_widths = mixed_flowable._colWidths
    checks.append((
        "서술형 열(0)이 짧은 '예/아니오' 열(1)보다 훨씬 넓다",
        mixed_widths[0] > mixed_widths[1] * 3,
    ))
    checks.append((
        "열 너비 합이 frame_width와 같다(빈 공간 없이 꽉 참)",
        abs(sum(mixed_widths) - frame_width) < 1,
    ))
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```
venv/Scripts/python.exe test_export_pdf.py
```
예상: 새 두 체크가 `FAIL`. 지금은 `_build_table_element`가 `document_styles`의 정적 비율만 쓰므로, "이행여부" 열이 "확인항목" 열보다 넓거나 비슷하게 나올 것이다(현재 스펙엔 `TBM 일지`가 `[10, 22, 34, 16]`로 정의돼 있고 2열 표라 앞 2개 값 `[10, 22]`를 쓰게 됨 — 즉 1열:2열 = 10:22로 오히려 "예/아니오" 열이 더 넓게 나와 첫 체크가 확실히 FAIL한다).

- [ ] **Step 3: 헬퍼 함수 구현**

`export_pdf.py`에서 `import` 블록에 `re` 추가:

```python
import io
import re
from xml.sax.saxutils import escape
```

`_BASE_TABLE_STYLE_COMMANDS` 정의 아래, `_hex_color` 함수 위에 추가:

```python
_MIN_COL_WIDTH_PT = 30
_CELL_H_PADDING_PT = 8
_LINE_BREAK_RE = re.compile(r"<br\s*/?>|\n")


def _measure_max_line_width(text, font_size):
    """
    셀 텍스트에 <br>/개행이 있으면 줄 단위로 나눠 가장 긴 한 줄의 렌더 폭을,
    없으면 전체 텍스트의 폭을 그대로 잰다. 여러 줄짜리 셀을 한 줄로 보고
    폭을 과대 측정하는 걸 막기 위함이다.
    """
    lines = _LINE_BREAK_RE.split(text) if text else [""]
    if not lines:
        lines = [""]
    return max(pdfmetrics.stringWidth(line, _FONT_NAME, font_size) for line in lines)


def _content_aware_col_widths(raw_rows, ncols, frame_width, font_size):
    """
    각 열의 '자연 폭'(그 열에서 가장 긴 한 줄의 렌더 폭 + 좌우 여백)을 측정해
    열 너비를 정한다. 모든 열에 최소폭(_MIN_COL_WIDTH_PT)을 먼저 보장하고,
    남는 공간을 각 열이 최소폭을 넘어 실제로 더 필요로 하는 만큼에 비례해서만
    나눠준다 — "예/아니오"처럼 짧은 열은 최소폭에 머물고, 서술형 열이 남는
    공간을 가져간다. 특정 한 열의 자연폭이 아무리 커도 가져갈 수 있는 몫은
    '남는 공간' 전체를 넘지 못하므로, 다른 열의 최소폭을 침해할 수 없다
    (요구사항 5: 셀 하나 때문에 표가 페이지 폭을 넘는 걸 구조적으로 방지).
    합계는 항상 frame_width와 같다.
    """
    natural = []
    for col in range(ncols):
        widest = 0.0
        for row in raw_rows:
            text = row[col] if col < len(row) else ""
            widest = max(widest, _measure_max_line_width(text, font_size))
        natural.append(widest + _CELL_H_PADDING_PT)

    floor = min(_MIN_COL_WIDTH_PT, frame_width / ncols)
    reserved = floor * ncols
    surplus = max(frame_width - reserved, 0)
    extra_need = [max(w - floor, 0) for w in natural]
    total_extra_need = sum(extra_need)

    if total_extra_need == 0:
        return [floor + surplus / ncols] * ncols

    return [floor + surplus * (need / total_extra_need) for need in extra_need]
```

`_build_table_element` 안에서 기존:

```python
    weights = resolve_column_weights(style, ncols)
    total_weight = sum(weights)
    col_widths = [frame_width * w / total_weight for w in weights]
```

를 아래로 교체:

```python
    col_widths = _content_aware_col_widths(table, ncols, frame_width, _CELL_STYLE_LEFT.fontSize)
```

(`resolve_column_weights` import는 XLSX/HWPX가 여전히 쓰므로 `document_styles`에서 지우지 않는다 — `export_pdf.py`의 import 목록에서 `resolve_column_weights`만 더 이상 쓰이지 않게 되는데, 그대로 둬도 동작엔 문제 없지만 미사용 import이니 지운다.)

`document_styles` import 줄에서 `resolve_column_weights` 제거:

```python
from document_styles import (
    AI_SCORE_FOOTNOTE, base_header, cell_style_decision, get_style,
    parse_ai_score_cell, risk_grade_column_indices,
)
```

- [ ] **Step 4: 테스트 실행 — 통과 확인**

```
venv/Scripts/python.exe test_export_pdf.py
```
예상: 전체 PASS(기존 체크 포함 — 특히 "좁은 열에 긴 텍스트가 와도 PDF 생성이 실패하지 않음"도 여전히 PASS해야 한다. content-aware 배분으로도 그 12열 표의 1열은 여전히 상대적으로 좁을 수 있으므로 `_fit_cell_text` 안전장치는 그대로 유지된다).

- [ ] **Step 5: 교차 포맷 일관성 테스트를 새 동작에 맞게 수정**

`test_export_style_consistency.py`의 아래 블록(104~118행 부근, "PDF: 열너비 '비율'이 스펙과 일치하는지" 주석부터 `checks.append(("PDF: 열너비 비율이 스펙(column_widths)과 일치", pdf_ratios_ok))`까지)을 통째로 교체:

```python
    # PDF: 열너비는 더 이상 document_styles의 정적 비율을 따르지 않는다
    # (2026-08-04부터 PDF만 실제 셀 내용 길이 기반 content-aware 배분으로 전환 —
    # docs/superpowers/plans/2026-08-04-pdf-서식-개선.md 참고). XLSX/HWPX는
    # 계속 정적 비율을 쓰므로 이 표만 셋의 열비율이 서로 달라도 정상이다.
    # 대신 PDF가 지켜야 할 불변식만 검증한다: 열 너비 합 == frame_width.
    pdf_col_widths = table_flowable._colWidths
    pdf_ncols = max(len(row) for row in tables[0])
    checks.append((
        "PDF: 열 개수만큼 너비가 계산됨",
        len(pdf_col_widths) == pdf_ncols,
    ))
    checks.append((
        "PDF: 열너비 합이 frame_width와 일치(content-aware 배분, 정적 비율 아님)",
        abs(sum(pdf_col_widths) - frame_width) < 1,
    ))
```

- [ ] **Step 6: 테스트 실행 — 통과 확인**

```
venv/Scripts/python.exe test_export_style_consistency.py
```
예상: 전체 PASS.

- [ ] **Step 7: 커밋**

```bash
git add export_pdf.py test_export_pdf.py test_export_style_consistency.py
git commit -m "feat: PDF 표 열너비를 실제 셀 내용 길이 기반 content-aware 배분으로 전환"
```

---

### Task 2: 문서 제목 — 가운데정렬 + 밑줄 + 28pt + 굵게 (요구사항 1)

**Files:**
- Modify: `export_pdf.py` (`_TitleFlowable` 클래스 추가, `record_to_pdf_bytes`에서 사용)
- Modify: `test_export_pdf.py` (새 테스트 함수)

**Interfaces:**
- Produces: `_TitleFlowable(text: str)` 클래스, `_center_x(text_width: float, avail_width: float) -> float` 순수 함수(테스트용으로 분리)
- Consumes: 기존 `_FONT_NAME`, `pdfmetrics.stringWidth`, `reportlab.platypus.Flowable`

- [ ] **Step 1: 실패하는 테스트 작성**

`test_export_pdf.py` 맨 위 import 블록에 추가:

```python
from export_pdf import _build_table_element, _center_x, _TitleFlowable, record_to_pdf_bytes
```

(기존 `from export_pdf import _build_table_element, record_to_pdf_bytes` 줄을 이렇게 바꾼다.)

`run()` 함수 맨 앞부분, `checks = []` 바로 뒤에 추가:

```python
    # --- 문서 제목: 가운데정렬 + 밑줄 + 28pt + 굵게(faux-bold 2회 겹쳐 그리기) ---
    checks.append(("제목 폰트 크기 28pt", _TitleFlowable("위험성평가표").font_size == 28))
    checks.append(("가운데정렬 계산: 텍스트 폭이 가용폭보다 작을 때 중앙 배치", _center_x(100, 300) == 100))
    checks.append(("가운데정렬 계산: 텍스트가 가용폭보다 크면 0에서 시작(음수 금지)", _center_x(400, 300) == 0))

    class _FakeCanvas:
        def __init__(self):
            self.draw_calls = []
            self.line_calls = []
        def setFont(self, *a, **k): pass
        def setLineWidth(self, *a, **k): pass
        def drawString(self, x, y, text): self.draw_calls.append((x, y, text))
        def line(self, *a, **k): self.line_calls.append(a)

    title_flowable = _TitleFlowable("표준 작업계획서")
    title_flowable.wrap(500, 1000)
    fake_canvas = _FakeCanvas()
    title_flowable.canv = fake_canvas
    title_flowable.draw()
    checks.append(("제목을 두 번 겹쳐 그려 굵게 보이게 함(faux-bold)", len(fake_canvas.draw_calls) == 2))
    checks.append((
        "두 번 그린 텍스트 내용이 같다(약간의 x 오프셋만 다름)",
        fake_canvas.draw_calls[0][2] == fake_canvas.draw_calls[1][2] == "표준 작업계획서",
    ))
    checks.append(("밑줄이 한 번 그려짐", len(fake_canvas.line_calls) == 1))
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```
venv/Scripts/python.exe test_export_pdf.py
```
예상: `ImportError: cannot import name '_center_x'` (또는 `_TitleFlowable`) — 아직 없으므로 기능 미구현으로 실패.

- [ ] **Step 3: `_TitleFlowable` 구현**

`export_pdf.py`에서 import 블록의 `from reportlab.platypus import ...`에 `Flowable` 추가:

```python
from reportlab.platypus import Flowable, Paragraph, SimpleDocTemplate, Spacer, Table, TableStyle
```

`_FOOTNOTE_STYLE` 정의 아래, `_BASE_TABLE_STYLE_COMMANDS` 위에 추가:

```python
_TITLE_FONT_SIZE = 28


def _center_x(text_width, avail_width):
    """가운데 정렬 x좌표. 텍스트가 가용폭보다 넓으면 0(음수 좌표 방지)."""
    return max((avail_width - text_width) / 2, 0)


class _TitleFlowable(Flowable):
    """
    문서 제목: 가운데정렬 + 밑줄 + 28pt + 굵게.
    CID 폰트(HYSMyeongJo-Medium)에는 검증된 Bold 변형이 없어(파일 상단 설명
    참고), 같은 텍스트를 아주 살짝(0.4pt) 겹쳐서 두 번 그리는 faux-bold로
    굵게 보이게 한다. 밑줄은 텍스트 폭만큼 직접 선을 그어 구현한다(Paragraph의
    <u> 태그도 가능하지만, 굵게 처리를 위해 애초에 Paragraph를 안 쓰므로
    통일해서 canvas로 직접 그린다).
    """

    def __init__(self, text, font_name=_FONT_NAME, font_size=_TITLE_FONT_SIZE):
        super().__init__()
        self.text = text
        self.font_name = font_name
        self.font_size = font_size
        self._text_width = pdfmetrics.stringWidth(text, font_name, font_size)
        self._bold_offset = 0.4
        self.width = 0
        self.height = font_size * 1.6

    def wrap(self, availWidth, availHeight):
        self.width = availWidth
        return availWidth, self.height

    def draw(self):
        canv = self.canv
        canv.setFont(self.font_name, self.font_size)
        x = _center_x(self._text_width, self.width)
        baseline_y = self.height - self.font_size
        canv.drawString(x, baseline_y, self.text)
        canv.drawString(x + self._bold_offset, baseline_y, self.text)
        underline_y = baseline_y - 4
        canv.setLineWidth(1.1)
        canv.line(x, underline_y, x + self._text_width + self._bold_offset, underline_y)
```

`record_to_pdf_bytes` 안에서 기존:

```python
    elements = [Paragraph(escape(document_type), _TITLE_STYLE), Spacer(1, 12)]
```

를 아래로 교체:

```python
    elements = [_TitleFlowable(document_type), Spacer(1, 12)]
```

(`_TITLE_STYLE`은 더 이상 안 쓰이므로 정의를 지운다: `_TITLE_STYLE = ParagraphStyle(...)` 줄 삭제.)

- [ ] **Step 4: 테스트 실행 — 통과 확인**

```
venv/Scripts/python.exe test_export_pdf.py
```
예상: 전체 PASS. 기존 "문서종류 제목 보존"(`SAMPLE_RECORD["document_type"] in full_text`) 체크도 계속 PASS해야 한다 — `drawString`으로 그린 텍스트도 pypdf가 정상 추출한다(두 번 겹쳐 그려도 부분 문자열 포함 여부만 보므로 영향 없음).

- [ ] **Step 5: 커밋**

```bash
git add export_pdf.py test_export_pdf.py
git commit -m "feat: PDF 문서 제목을 가운데정렬·밑줄·28pt·굵게(faux-bold)로 변경"
```

---

### Task 3: 마크다운에서 헤딩(박스 제목)까지 순서대로 뽑아내는 파서 추가

**Files:**
- Modify: `markdown_tables.py` (새 함수 추가, 기존 `parse_markdown_tables`는 그대로 둠 — XLSX/HWPX가 계속 씀)
- Create: `test_markdown_blocks.py`

**Interfaces:**
- Produces: `parse_markdown_blocks(markdown_text: str) -> list[dict]`. 각 dict는 `{"type": "heading", "text": str}` 또는 `{"type": "table", "rows": list[list[str]]}`.
- Consumes: 없음(기존 `_TABLE_ROW_RE`, `_SEPARATOR_CELL_RE`, `_clean_cell`, `_split_row`, `_is_separator_row` 재사용)

배경: 실제 생성되는 draft는 `## ■ 위험성평가표 (정보통신공사)`, `### 작업 1. 굴착 작업 중 지반 붕괴`처럼 레벨 2~3 헤딩이 표 바로 앞에 온다(레벨 1 `# 위험성평가표 초안`은 문서 제목과 중복이라 제외). 지금 `parse_markdown_tables`는 표가 아닌 텍스트를 전부 버리므로 이 헤딩들이 사라진다. PDF 전용으로 헤딩+표를 순서대로 보존하는 새 함수를 추가한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`test_markdown_blocks.py` 새로 작성:

```python
# -*- coding: utf-8 -*-
import sys
if hasattr(sys.stdout, "reconfigure"):
    sys.stdout.reconfigure(encoding="utf-8")

from markdown_tables import parse_markdown_blocks

SAMPLE = """# 위험성평가표 초안

---

## ■ 위험성평가표 (정보통신공사)

| 항목 | 내용 |
|------|------|
| 현장명 | 강남지사 |

---

### 작업 1. 굴착 작업 중 지반 붕괴

| 유해요인 | 대책 |
|------|------|
| 붕괴 | 흙막이 설치 |
"""


def run():
    checks = []
    blocks = parse_markdown_blocks(SAMPLE)

    checks.append(("레벨1 제목(# ...)은 블록에 안 들어감(문서 제목과 중복)", not any(
        b["type"] == "heading" and "초안" in b["text"] for b in blocks
    )))
    checks.append(("레벨2 헤딩이 heading 블록으로 들어감", any(
        b == {"type": "heading", "text": "■ 위험성평가표 (정보통신공사)"} for b in blocks
    )))
    checks.append(("레벨3 헤딩도 heading 블록으로 들어감", any(
        b == {"type": "heading", "text": "작업 1. 굴착 작업 중 지반 붕괴"} for b in blocks
    )))
    table_blocks = [b for b in blocks if b["type"] == "table"]
    checks.append(("표가 2개 들어감", len(table_blocks) == 2))
    checks.append(("첫 표 내용이 정확히 파싱됨", table_blocks[0]["rows"] == [["항목", "내용"], ["현장명", "강남지사"]]))
    checks.append(("순서 보존: heading -> table -> heading -> table", [b["type"] for b in blocks] == [
        "heading", "table", "heading", "table",
    ]))

    print()
    all_ok = True
    for name, ok in checks:
        print(f"[{'PASS' if ok else 'FAIL'}] {name}")
        all_ok = all_ok and ok
    print("\n전체 결과:", "PASS" if all_ok else "FAIL (위 로그 확인)")
    return all_ok


if __name__ == "__main__":
    run()
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```
venv/Scripts/python.exe test_markdown_blocks.py
```
예상: `ImportError: cannot import name 'parse_markdown_blocks'`.

- [ ] **Step 3: `parse_markdown_blocks` 구현**

`markdown_tables.py`의 `_BOLD_RE` 정의 아래에 추가:

```python
_HEADING_RE = re.compile(r"^#{2,6}\s+(.+?)\s*$")
```

`parse_markdown_tables` 함수 뒤(파일 끝)에 추가:

```python
def parse_markdown_blocks(markdown_text):
    """
    markdown_text를 헤딩(레벨 2~6)과 표를 순서대로 보존한 블록 리스트로
    반환한다. parse_markdown_tables와 달리 헤딩 텍스트를 버리지 않는다 —
    PDF가 표 앞에 "박스 제목"을 그리기 위해 필요하다(export_pdf.py 전용,
    XLSX/HWPX는 계속 parse_markdown_tables를 쓴다).
    레벨 1(# ...)은 문서 전체 제목과 중복이라 제외한다.
    """
    blocks = []
    current_rows = []

    def flush_table():
        if current_rows:
            blocks.append({"type": "table", "rows": current_rows[:]})
            current_rows.clear()

    for line in markdown_text.splitlines():
        table_match = _TABLE_ROW_RE.match(line)
        if table_match:
            cells = _split_row(line)
            if _is_separator_row(cells):
                continue
            current_rows.append([_clean_cell(c) for c in cells])
            continue

        flush_table()

        heading_match = _HEADING_RE.match(line)
        if heading_match:
            blocks.append({"type": "heading", "text": heading_match.group(1).strip()})

    flush_table()
    return blocks
```

- [ ] **Step 4: 테스트 실행 — 통과 확인**

```
venv/Scripts/python.exe test_markdown_blocks.py
```
예상: 전체 PASS.

- [ ] **Step 5: 커밋**

```bash
git add markdown_tables.py test_markdown_blocks.py
git commit -m "feat: 마크다운 헤딩+표를 순서대로 보존하는 parse_markdown_blocks 추가"
```

---

### Task 4: PDF에 박스 제목 렌더링 + 페이지 분할 시 제목-표 분리 방지 (요구사항 2, 4)

**Files:**
- Modify: `export_pdf.py` (`record_to_pdf_bytes`가 `parse_markdown_blocks` 사용하도록 교체, 박스 제목 스타일 추가)
- Modify: `test_export_pdf.py`

**Interfaces:**
- Consumes: `parse_markdown_blocks` (Task 3), `_build_table_element` (기존)

배경: `repeatRows=1`은 이미 설정돼 있어(요구사항 4의 "표가 페이지 넘어가면 열 제목만 반복") 표 하나가 길어서 페이지를 넘기는 경우는 이미 처리된다. 다만 지금은 "박스 제목"(헤딩) 자체가 안 그려지므로 요구사항 2가 우선 필요하고, 헤딩이 페이지 맨 아래에 혼자 남고 표가 다음 페이지로 넘어가는 것도 보기 안 좋으므로 `keepWithNext=True`로 방지한다(표 자체가 한 페이지보다 길면 어차피 어딘가에서는 끊어야 하므로, `keepWithNext`는 무리하게 강제하지 않고 "가능하면" 붙여준다 — reportlab의 정상 동작이며 `KeepTogether`처럼 억지로 붙이려다 LayoutError를 다시 일으키지 않는다).

- [ ] **Step 1: 실패하는 테스트 작성**

`test_export_pdf.py`에 아래 레코드를 `SAMPLE_RECORD_MIXED_WIDTH` 아래에 추가:

```python
SAMPLE_RECORD_WITH_HEADING = {
    "document_type": "위험성평가표",
    "draft": (
        "# 위험성평가표 초안\n\n"
        "## ■ 기본 정보\n\n"
        "| 항목 | 내용 |\n"
        "|------|------|\n"
        "| 현장명 | 강남지사 |\n\n"
        "## ■ 중점(One Point) 위험요인\n\n"
        "| 위험요인 | 대책 |\n"
        "|------|------|\n"
        "| 붕괴 | 흙막이 설치 |\n"
    ),
}
```

`run()` 안, Task 1에서 추가한 마지막 체크(열너비 합) 뒤에 추가:

```python
    # --- 박스 제목(헤딩)이 PDF에 실제로 그려짐 ---
    heading_pdf = record_to_pdf_bytes(SAMPLE_RECORD_WITH_HEADING)
    heading_reader = pypdf.PdfReader(io.BytesIO(heading_pdf))
    heading_text = "".join(page.extract_text() for page in heading_reader.pages)
    checks.append(("박스 제목 '■ 기본 정보'가 PDF 본문에 포함됨", "기본 정보" in heading_text))
    checks.append(("박스 제목 '■ 중점(One Point) 위험요인'이 PDF 본문에 포함됨", "중점" in heading_text and "위험요인" in heading_text))
    checks.append(("레벨1 제목('위험성평가표 초안')은 본문에 중복 삽입되지 않음(문서 제목에서만 1번)", heading_text.count("초안") == 0))
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```
venv/Scripts/python.exe test_export_pdf.py
```
예상: 박스 제목 관련 2개 체크 FAIL(지금은 `record_to_pdf_bytes`가 `parse_markdown_tables`만 써서 헤딩이 통째로 사라짐).

- [ ] **Step 3: 박스 제목 스타일 및 렌더링 로직 구현**

`export_pdf.py` import 줄 수정:

```python
from markdown_tables import parse_markdown_blocks
```

(기존 `from markdown_tables import parse_markdown_tables` 를 이렇게 교체 — `export_xlsx.py`/`export_hwpx.py`는 별도 파일이라 영향 없음.)

`_FOOTNOTE_STYLE` 정의 바로 아래에 박스 제목 스타일 추가:

```python
_BOX_TITLE_STYLE = ParagraphStyle(
    "box_title", fontName=_FONT_NAME, fontSize=18, leading=22,
    alignment=TA_LEFT, spaceBefore=14, spaceAfter=6, keepWithNext=True,
)
```

`record_to_pdf_bytes` 함수 전체를 아래로 교체:

```python
def record_to_pdf_bytes(record):
    """
    record["draft"]에서 헤딩(박스 제목)과 Markdown 표를 순서대로 파싱해
    문서를 만든다. 표가 하나도 없으면 draft 원문을 문단 하나로 그대로
    넣는다(XLSX/HWPX와 동일 규칙). 표 셀은 Paragraph로 감싸 렌더링하므로
    원문을 그대로 넣기 전에 escape()한다.
    """
    blocks = parse_markdown_blocks(record["draft"])
    document_type = record["document_type"]

    buffer = io.BytesIO()
    doc = SimpleDocTemplate(
        buffer, pagesize=landscape(A4),
        title=document_type, author="Safety-RAG",
    )
    elements = [_TitleFlowable(document_type), Spacer(1, 12)]

    table_blocks = [b for b in blocks if b["type"] == "table"]
    if not table_blocks:
        body_text = escape(record["draft"]).replace("\n", "<br/>")
        elements.append(Paragraph(body_text, _BODY_STYLE))
    else:
        for block in blocks:
            if block["type"] == "heading":
                elements.append(Paragraph(escape(block["text"]), _BOX_TITLE_STYLE))
                continue

            table_flowable, ai_value_present = _build_table_element(
                block["rows"], doc.width, document_type
            )
            elements.append(table_flowable)
            if ai_value_present:
                elements.append(Spacer(1, 4))
                elements.append(Paragraph(AI_SCORE_FOOTNOTE, _FOOTNOTE_STYLE))
            elements.append(Spacer(1, 12))

    doc.build(elements)
    return buffer.getvalue()
```

- [ ] **Step 4: 테스트 실행 — 통과 확인**

```
venv/Scripts/python.exe test_export_pdf.py
```
예상: 전체 PASS.

- [ ] **Step 5: 전체 회귀 테스트 실행(다른 export_pdf 소비자 확인)**

```
venv/Scripts/python.exe test_export_style_consistency.py
venv/Scripts/python.exe test_export_routes.py
```
예상: 전체 PASS(두 파일 다 `parse_markdown_tables`를 직접 쓰거나 `_build_table_element`에 표 rows를 직접 넘기므로, `record_to_pdf_bytes` 내부 변경과 무관하다 — 단, `test_export_routes.py`는 `record_to_pdf_bytes`를 라우트 통해 간접 호출하므로 여기서 실제로 검증된다).

- [ ] **Step 6: 커밋**

```bash
git add export_pdf.py test_export_pdf.py
git commit -m "feat: PDF에 박스 제목(헤딩) 렌더링 추가, 표와 keepWithNext로 분리 방지"
```

---

### Task 5: 표가 페이지를 넘어갈 때 열 제목이 반복되는지 명시적으로 검증 (요구사항 4)

**Files:**
- Modify: `test_export_pdf.py`

**Interfaces:** 없음(기존 `repeatRows=1` 동작을 검증만 함, 구현 변경 없음 — 이미 Task 1의 `_build_table_element` 안에 `repeatRows=1`이 그대로 남아있다).

- [ ] **Step 1: 여러 페이지에 걸치는 표를 만들어 각 페이지에 헤더 행이 나오는지 확인하는 테스트 추가**

`test_export_pdf.py`의 `run()` 안, Task 4에서 추가한 체크들 뒤에 추가:

```python
    # --- 표가 여러 페이지에 걸치면 헤더 행(열 제목)이 각 페이지에 반복됨 ---
    many_rows_lines = ["| 번호 | 위험요인 | 감소대책 |", "|------|------|------|"]
    for i in range(80):
        many_rows_lines.append(f"| {i+1} | 위험요인 {i+1} | 감소대책 상세 설명 {i+1} |")
    many_rows_record = {
        "document_type": "위험성평가표",
        "draft": "\n".join(many_rows_lines),
    }
    many_rows_pdf = record_to_pdf_bytes(many_rows_record)
    many_rows_reader = pypdf.PdfReader(io.BytesIO(many_rows_pdf))
    checks.append(("80행 표는 여러 페이지로 분할됨", len(many_rows_reader.pages) > 1))
    pages_with_header = sum(
        1 for page in many_rows_reader.pages if "위험요인" in page.extract_text() and "감소대책" in page.extract_text()
    )
    checks.append(("헤더 행(열 제목)이 모든 페이지에 반복됨", pages_with_header == len(many_rows_reader.pages)))
```

- [ ] **Step 2: 테스트 실행 — 통과 확인 (이미 구현돼 있으므로 바로 GREEN이어야 함)**

```
venv/Scripts/python.exe test_export_pdf.py
```
예상: 전체 PASS. 만약 "헤더 행이 모든 페이지에 반복됨"이 FAIL하면 — Task 1~4를 거치며 `repeatRows=1`이 실수로 지워졌는지 `_build_table_element`의 `Table(...)` 생성 부분을 확인할 것(다른 코드 변경 없이 이 테스트만 추가하는 태스크이므로, FAIL 시 이전 태스크에서의 실수를 의심해야 한다 — 새 기능 추가가 아니라 회귀 검증이다).

- [ ] **Step 3: 커밋**

```bash
git add test_export_pdf.py
git commit -m "test: PDF 표가 여러 페이지로 나뉠 때 헤더 행 반복을 명시적으로 검증"
```

---

### Task 6: 3개 문서유형 모두 A4 페이지 크기로 통일되는지 검증 (요구사항 6)

**Files:**
- Modify: `test_export_pdf.py`

**Interfaces:** 없음(현재 `record_to_pdf_bytes`가 이미 모든 문서유형에 `pagesize=landscape(A4)`를 무조건 적용하므로, 구현 변경 없이 검증만 추가한다).

- [ ] **Step 1: 3개 문서유형 각각 페이지 크기가 landscape A4와 일치하는지 확인하는 테스트 추가**

`test_export_pdf.py` 맨 위 import에 `landscape` 관련해서 이미 `from reportlab.lib.pagesizes import A4, landscape`가 있으므로 추가 import 불필요. `run()` 안, 마지막 체크 뒤에 추가:

```python
    # --- 3개 문서유형 모두 A4(가로) 페이지 크기로 통일됨 ---
    expected_size = landscape(A4)
    for doc_type, draft in [
        ("위험성평가표", "| 항목 | 내용 |\n|------|------|\n| 현장명 | 강남 |\n"),
        ("표준 작업계획서", "| 항목 | 내용 |\n|------|------|\n| 작업유형 | 굴착작업 |\n"),
        ("TBM 일지", "| 항목 | 내용 |\n|------|------|\n| 일자 | 2026-08-04 |\n"),
    ]:
        doc_pdf = record_to_pdf_bytes({"document_type": doc_type, "draft": draft})
        doc_reader = pypdf.PdfReader(io.BytesIO(doc_pdf))
        box = doc_reader.pages[0].mediabox
        size_ok = abs(float(box.width) - expected_size[0]) < 1 and abs(float(box.height) - expected_size[1]) < 1
        checks.append((f"{doc_type} PDF 페이지 크기가 A4(가로)와 일치", size_ok))
```

- [ ] **Step 2: 테스트 실행 — 통과 확인 (이미 구현돼 있으므로 바로 GREEN이어야 함)**

```
venv/Scripts/python.exe test_export_pdf.py
```
예상: 전체 PASS. `record_to_pdf_bytes`가 `document_type`과 무관하게 항상 `pagesize=landscape(A4)`를 쓰므로 이미 만족돼 있어야 한다 — 이 태스크는 그 사실을 회귀 테스트로 못박는 것이 목적이다.

- [ ] **Step 3: 커밋**

```bash
git add test_export_pdf.py
git commit -m "test: 3개 문서유형 PDF가 모두 A4(가로) 페이지 크기로 통일됨을 검증"
```

---

## 최종 확인 (모든 태스크 완료 후)

```bash
venv/Scripts/python.exe test_export_pdf.py
venv/Scripts/python.exe test_export_style_consistency.py
venv/Scripts/python.exe test_export_routes.py
venv/Scripts/python.exe test_markdown_blocks.py
```

전체 PASS 확인 후, 사용자에게 실제 PDF 파일 하나를 받아 육안으로 확인받을 것(자동 테스트는 텍스트 존재·치수·비율만 검증하고 실제 "보기 좋은지"는 못 봄 — 특히 faux-bold가 실제로 굵어 보이는지, 박스 제목 폰트가 18pt로 적절히 커 보이는지는 사람이 확인해야 한다).
